# Prod namespace for the project.
apiVersion: v1
kind: Namespace
metadata:
  name: proj-scoutid-prod
  labels:
    scouterna.se/project: proj-scoutid
    scouterna.se/env: prod
    # Same level as dev on purpose — see docs/security.md.
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/enforce-version: v1.36
    pod-security.kubernetes.io/warn: restricted
    pod-security.kubernetes.io/audit: restricted
    # Opt in to the shared Key Vault store — this namespace has a database.
    # Grants the WHOLE vault: docs/security.md §3.
    scouterna.se/keyvault-access: "true"
